<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://cyrusknopf.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://cyrusknopf.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-07T20:09:51+00:00</updated><id>https://cyrusknopf.com/feed.xml</id><title type="html">Cyrus Knopf</title><subtitle>Cyrus Knopf. </subtitle><entry><title type="html">Codeforces 2162C - Asymptotic and practical improvement over editorial</title><link href="https://cyrusknopf.com/posts/2025/2162c-asym-improvement/" rel="alternate" type="text/html" title="Codeforces 2162C - Asymptotic and practical improvement over editorial"/><published>2025-02-07T07:33:00+00:00</published><updated>2025-02-07T07:33:00+00:00</updated><id>https://cyrusknopf.com/posts/2025/2162c-asym-improvement</id><content type="html" xml:base="https://cyrusknopf.com/posts/2025/2162c-asym-improvement/"><![CDATA[<p><a href="https://codeforces.com/problemset/problem/2162/C">Codeforces Problem 2162C</a> reads as follows:</p> <hr/> <p>You are given two integers \(a\) and \(b\). You are allowed to perform the following operation any number of times (including zero):</p> <ul> <li>choose any integer \(x\) such that \(0\le x\le a\) (the current value of \(a\), not initial),</li> <li>set \(a:=a \oplus x\). Here, \(\oplus\) represents the bitwise XOR operator.</li> </ul> <p>After performing a sequence of operations, you want the value of \(a\) to become exactly \(b\).</p> <p>Find a sequence of at most 100 operations (values of \(x\) used in each operation) that transforms \(a\) into \(b\), or report that it is impossible.</p> <hr/> <p>The rest of this post will discuss solutions to this problem, so attempt the problem before reading ahead, if you’d like to.</p> <p>The proposed <a href="https://codeforces.com/blog/entry/147242">editorial solution</a>, courtesy of user <a href="https://codeforces.com/profile/wakanda-forever">wakanda-forever</a>, involves iterating over each bit of \(a\) and setting that bit of \(x\) to high, and then unsetting bits in \(b\) which are low. This guarantees a solution in fewer than 60 operations (due to the size constraints on \(a\) and \(b\)). Since we are iterating over the bits, the author claims a complexity of \(O(\log_2(a))\) (I normally see complexity implicitly be in terms of the size of the binary representation of inputs, so I would’ve said \(O(a)\)). The solution I’ll present below is \(O(1)\) and guarantees a solution in 2 or fewer operations.</p> <p>Immediately, we realise that if \(\text{msb}(a) &lt; \text{msb}(b)\), then we cannot make \(b\) from XORing \(a\) with \(x\), since \(x \le a\) implies \(\text{msb}(x)\le \text{msb}(a)&lt; \text{msb}(b)\), and so we can never set the highest bit(s) of \(b\). So if we have \(\text{msb}(a) &lt; \text{msb}(b)\), we can early return with <code class="language-plaintext highlighter-rouge">-1</code>. Otherwise, we know that \(\text{msb}(b) \le \text{msb}(a)\).</p> <p>Our goal is to find \(x\) such that \(a \oplus x = b\). We can use the cool property of XOR being that it is its own inverse, so from;</p> \[a \oplus x = b\] <p>we have;</p> \[x = a \oplus b\] <p>but let’s convince ourselves. Applying \(a\oplus\cdot\) to both sides;</p> \[a \oplus (a \oplus x) = a\oplus (b)\] <p>Since XOR is associative, we have;</p> \[(a \oplus a) \oplus x = a \oplus (b)\] <p>and the XOR of any \(y\) with itself is 0, so from the above we have;</p> \[0 \oplus x = a \oplus (b)\] <p>…and the XOR of any \(z\) with 0 is just \(z\), so;</p> \[x = a\oplus b\] <p>So setting \(x = a \oplus b\) will give us \(a\otimes x = b\).</p> <p>But there is a catch: this \(x\) is not always valid. Consider \(a = 9, b = 6\) gives us \(x = 15\), which checks out: \(9 \oplus 15 = 6\), but does not fit the other constraint of \(x \le a\).</p> <p>Luckily, we only have two cases to deal with:</p> <p><strong>Case 1</strong>: \(x \le a\)</p> <p>We can solve in single operation: \(x\).</p> <p><strong>Case 2</strong>: \(a &lt; x\)</p> <p>We know that \(\text{msb}(x)\le \text{msb}(a)\) from above, but since \(a &lt; x\), we have the stronger condition of \(\text{msb}(x) = \text{msb}(a)\)<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>.</p> <p>Let \(x'\) be \(x\) with its most significant bit set low, i.e. \(x' = x\ \&amp;\ \lnot\text{msb}(x)\) <sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>, equivalently \(\text{msb}(x) \mathbin{\&amp;} x' = x\).</p> <p>And since \(\text{msb}(x') &lt; \text{msb}(x) \le \text{msb}(a)\), we have \(x' &lt; a\) and \(\text{msb}(x) = \text{msb}(a) \le a\), meaning we can return a vector of \(\{ \text{msb}(x), x' \}\) as our solution.</p> <blockquote> <p>We have proved that we never need more than 2 operations to solve the problem.</p> </blockquote> <p>We can implement this in C++ with bit manipulation. I use a typedef of <code class="language-plaintext highlighter-rouge">num = uint64_t</code> in the snippets below.</p> <p>To check whether \(\text{msb}(a) &lt; \text{msb}(b)\) and whether the problem is solvable, we use compiler intrinsics to “count leading zeros” of \(a\) and \(b\):</p> <pre><code class="language-null">if (__builtin_clzll(b) &lt; __builtin_clzll(a)) {
    return {"-1"};
}
</code></pre> <p>we then compute our \(x\);</p> <pre><code class="language-null">num x = a ^ b;
</code></pre> <p>check whether we can solve in a single operation;</p> <pre><code class="language-null">if (x &lt;= a) {
    string out = "1\n" + to_string(x);
    return out;
}
</code></pre> <p>use some more bit tricks to compute our \(x'\), and then return our two operations;</p> <pre><code class="language-null">// get the msb of x
num msbit = 1ull &lt;&lt; ((sizeof(num) * 8 - __builtin_clzll(x)) - 1);
// remove the msb from x to get x'
x ^= msbit;

string out = "2\n" + to_string(x) + ' ' + to_string(msbit);
return out;
</code></pre> <p>The complete solution, with an initial check for trivial test cases, is:</p> <pre><code class="language-null">using namespace stld;
using num = uint6l4_t;

string solve(num a, num b) {
    if (a == b) {
        return {"0"};
    }
    // if b has bits set high that are more significant than a's MSB, then we
    // can never set those high
    if (__builtin_clzll(b) &lt; __builtin_clzll(a)) {
        return {"-1"};
    }

    // get our single x
    num x = a ^ b;

    // if we can do it in one
    if (x &lt;= a) {
        string out = "1\n" + to_string(x);
        return out;
    }

    // get the msb of x
    num msbit = 1ull &lt;&lt; ((sizeof(num) * 8 - __builtin_clzll(x)) - 1);
    // remove the msb from x to get x'
    x ^= msbit;

    string out = "2\n" + to_string(x) + ' ' + to_string(msbit);
    return out;
}
</code></pre> <p>Not only is this solution \(O(1)\), but it is almost entirely bitwise operations and intrinsics, meaning it’s kind to the CPU. Ignoring constructing and returning the strings (only needed for codeforces I/O), <code class="language-plaintext highlighter-rouge">clang</code> generates fewer than 26 instructions to compute the answer (<a href="https://godbolt.org/z/f9bzajTeb">godbolt</a>).</p> <h1 id="footnotes">Footnotes</h1> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:1"> <p>Because \(y &lt; z \implies \text{msb}(y) \le\text{msb}(z)\) <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:2"> <p>I’m using \(\text{msb}(y)\) to be the binary representation of \(y\) with all bits other than its most significant set low, e.g. \(y = 5 = 0101\) would give \(\text{msb}(y) = 4 = 0100\). <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="codeforces"/><category term="cpp"/><category term="c++"/><category term="codeforces"/><category term="algorithms"/><summary type="html"><![CDATA[bit tricks to get a constant time solution]]></summary></entry></feed>